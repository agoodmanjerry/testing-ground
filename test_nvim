abde
abde
abde
abde
abde
abde
abde
abde
abde
abde
abde
abdce efef ii_wf oijef
abde

let g:clipboard = {
          \   'name': 'myClipboard',
          \   'copy': {
          \      '+': {lines, regtype -> extend(g:, {'foo': [lines, regtype]}) },
          \      '*': {lines, regtype -> extend(g:, {'foo': [lines, regtype]}) },
          \    },
          \   'paste': {
          \      '+': {-> get(g:, 'foo', [])},
          \      '*': {-> get(g:, 'foo', [])},
          \   },
          \ }

vim.g.clipboard = {
  'name': 'myClipboard',
  'copy': {
    '+': {lines, regtype -> extend(g:, {'foo': [lines, regtype]}) },
    '*': {lines, regtype -> extend(g:, {'foo': [lines, regtype]}) },
  },
  'paste': {
    '+': {-> get(g:, 'foo', [])},
    '*': {-> get(g:, 'foo', [])},
  }
}
 
fef
